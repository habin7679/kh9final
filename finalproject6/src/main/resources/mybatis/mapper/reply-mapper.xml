<?xml version="1.0" encoding="UTF-8"?>


<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
 <mapper namespace="reply">
 	
 	<select id="sequence" resultType="int">
 		select reply_seq.nextval from dual
 	</select>
 	
 	<insert id="insert" parameterType="ReplyDto">
 		insert into reply (reply_no, reply_target, member_no, reply_writer, reply_content, reply_time)
 			values (#{replyNo}, #{replyTarget}, #{memberNo}, #{replyWriter}, #{replyContent}, #{replyTime})
 	</insert>
 	
 	<select id="one" resultType="ReplyDto" parameterType="int">
 		select * from reply where reply_no = #{replyNo}
 	</select>
 	
 	<select id="list" parameterType="int" resultType="ReplyDto">
 		select * from reply where reply_target = #{replyTarget}
 	</select>
 	
 </mapper>